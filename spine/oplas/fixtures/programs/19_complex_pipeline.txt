(program (seq (let selected (select_components repr true)) (let mask (mask_from_objects repr selected)) (let recolored (recolor grid mask 0 1)) (let cropped (crop_to_bbox recolored selected)) (paste_at base cropped (rel top_left 0 0))))























