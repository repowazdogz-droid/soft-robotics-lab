import Foundation

enum ScenePreset: String, CaseIterable, Identifiable {
    case `default` = "Default"
    
    var id: String { rawValue }
    
    // Placeholder: add preset-specific data structures as needed
    // var roomNamePreset: RoomNamePreset { ... }
}

































