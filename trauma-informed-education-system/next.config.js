/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next 16: Turbopack is default; keep this empty unless you actually need turbopack options.
  turbopack: {},

  reactStrictMode: true,
};

module.exports = nextConfig;
