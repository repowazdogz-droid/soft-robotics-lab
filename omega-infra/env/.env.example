# Restic Repository Configuration
# REQUIRED: Must be s3: or sftp: (local repositories not allowed)

# Backblaze B2 (recommended)
RESTIC_REPOSITORY=s3:s3.us-west-000.backblazeb2.com/bucket-name/restic

# SSH Target (alternative)
# RESTIC_REPOSITORY=sftp:user@host:/path/to/restic

# Repository password (REQUIRED - generate strong password)
RESTIC_PASSWORD=change-me-generate-strong-password

# Backblaze B2 Credentials (required if using B2)
B2_ACCOUNT_ID=your-b2-account-id
B2_ACCOUNT_KEY=your-b2-account-key

# Backup Schedule (cron format)
BACKUP_SCHEDULE=0 2 * * *

# Retention Policy (fixed: 7 daily, 4 weekly, 6 monthly)
# RETENTION_DAILY=7
# RETENTION_WEEKLY=4
# RETENTION_MONTHLY=6

# Optional: Health check webhook (e.g., healthchecks.io)
HEALTHCHECK_WEBHOOK=

# Database Connection (for network access)
PGHOST=postgres
PGPORT=5432
PGUSER=postgres
PGPASSWORD=

REDIS_HOST=redis
REDIS_PORT=6379
