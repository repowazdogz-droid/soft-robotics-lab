import Foundation

enum CameraRig {
    // Vision Pro uses head pose; we still document limits for conceptual integrity.
    static let allowUserWalk: Bool = true
    static let allowObjectManipulation: Bool = false
}

